<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    
    <!-- Android Auto permission -->
    <uses-permission android:name="com.google.android.gms.permission.CAR_FUEL" />

    <!-- Dichiara che l'app supporta Android Auto -->
    <uses-feature
        android:name="android.hardware.type.automotive"
        android:required="false" />
        
    <uses-feature
        android:name="android.software.car.templates_host"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/Theme.FuelFinder"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />
            
        <!-- Android Auto metadata -->
        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            
            <!-- Android Auto launcher intent -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="com.google.android.gms.car.category.CATEGORY_PROJECTION" />
            </intent-filter>
            
            <!-- Supporto per Android Auto con Car App Library -->
            <meta-data android:name="distractionOptimized" android:value="true"/>
        </activity>

        <!-- Car Service per Android Auto -->
        <service
            android:name=".auto.FuelFinderCarAppService"
            android:exported="true"
            android:label="FuelFinder per Auto">
            <intent-filter>
                <action android:name="androidx.car.app.CarAppService" />
                <category android:name="androidx.car.app.category.POI" />
            </intent-filter>
        </service>

        <!-- WIDGET INTEGRATION: Widget Provider -->
        <receiver
            android:name=".widget.FuelFinderWidgetProvider"
            android:exported="true"
            android:label="FuelFinder Widget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.fuelfinder.app.UPDATE_WIDGET" />
                <action android:name="com.fuelfinder.app.NAVIGATE" />
                <action android:name="com.fuelfinder.app.TOGGLE_SORT" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_fuel_finder_info" />
        </receiver>

        <!-- WIDGET INTEGRATION: Widget Update Service -->
        <service
            android:name=".widget.WidgetUpdateService"
            android:exported="false" />

    </application>

</manifest>
